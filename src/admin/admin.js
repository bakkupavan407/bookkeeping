var bodyParser = require("body-parser");
var db = require("../../config/dbconfig").db;
var utils = require("../../utils/response");

module.exports = {
  savedepts: function( req, res ){
    
  }
};
